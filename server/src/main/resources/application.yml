server:
  port: 8788
  error:
    include-message: always

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${MYSQL_URL:jdbc:mysql://rm-2ze16xep4a5xsx1i5no.mysql.rds.aliyuncs.com:3306/whiteboard?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai}
    username: ${MYSQL_USERNAME:yima}
    password: ${MYSQL_PASSWORD:4rq57ukzBlIcNuuNGFqe8oOuIWkKhZUd}
  sql:
    init:
      # This service uses an external MySQL database. Spring otherwise skips
      # schema.sql, leaving recording completion without its session table.
      mode: always

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id

whiteboard:
  storage-root: ./data/storage/whiteboard
  qiniu:
    access-key: ${QINIU_ACCESS_KEY:7S_iVr-bCg2TwdpWuo86j1-yFWtTvuwYBY2Sds3H}
    secret-key: ${QINIU_SECRET_KEY:6jFw93Id_i9Zs0EL2UuO9ibasGttPpDu1jQ2i82N}
    bucket: ${QINIU_BUCKET:yima-h5}
    upload-url: ${QINIU_UPLOAD_URL:https://up-z1.qiniup.com}
    public-domain: ${QINIU_PUBLIC_DOMAIN:}
    token-expire-seconds: ${QINIU_TOKEN_EXPIRE_SECONDS:3600}

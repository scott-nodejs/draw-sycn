import { FormEvent, useState } from 'react'
import { GraduationCap, LoaderCircle } from 'lucide-react'
import { useStudentApi } from '../api'

export default function Auth(){const api=useStudentApi();const [mode,setMode]=useState<'login'|'register'>('login');const [account,setAccount]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{if(mode==='login')await api.login(account,password);else await api.register(account,password,name)}catch(cause){setError(cause instanceof Error?cause.message:'操作失败')}finally{setBusy(false)}};return <main className="min-h-screen bg-[#f5f8fc] flex items-center justify-center p-5"><section className="w-full max-w-[430px] rounded-2xl border border-slate-200 bg-white p-8 shadow-xl shadow-slate-200/40"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-blue-600 text-white"><GraduationCap/></span><div><h1 className="font-semibold text-slate-900">知问课堂</h1><p className="text-xs text-slate-400">学生端</p></div></div><h2 className="mt-8 text-2xl font-semibold">{mode==='login'?'学生登录':'注册学生账号'}</h2><p className="mt-1 text-sm text-slate-400">登录后查看老师下发的任务和同步课堂</p><form onSubmit={submit} className="mt-6 space-y-4">{mode==='register'?<label className="block text-sm text-slate-600">姓名<input required value={name} onChange={e=>setName(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-4 outline-none focus:border-blue-400"/></label>:null}<label className="block text-sm text-slate-600">手机号<input required value={account} onChange={e=>setAccount(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-4 outline-none focus:border-blue-400"/></label><label className="block text-sm text-slate-600">密码<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-slate-200 px-4 outline-none focus:border-blue-400"/></label>{error?<p className="text-sm text-red-500">{error}</p>:null}<button disabled={busy} className="btn-primary w-full">{busy?<LoaderCircle size={17} className="animate-spin"/>:null}{mode==='login'?'登录':'注册并登录'}</button></form><button onClick={()=>{setMode(mode==='login'?'register':'login');setError('')}} className="mt-5 w-full text-sm text-blue-600">{mode==='login'?'没有账号？立即注册':'已有账号？返回登录'}</button></section></main>}
